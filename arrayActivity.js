// arrayActivity.js

// Step 1: Create an array called favSingers and store your three favorite singers.
const favSingers = ["Adele", "Joji", "billie Eilish"];

// Log the first singer in the array using array index notation.
console.log(favSingers[0]);  // Expected output: Adele

// Step 2: Create an array called favNumbers and store four of your favorite numbers.
const favNumbers = [7, 3, 21, 42];

// Step 3: Create an array named mixedArr and store different data types.
const mixedArr = ["string", ["otherarray"], 123, true];

// Step 4: Access and log each item in the mixedArr using array index notation.
console.log(mixedArr[0]);  // Expected output: "string"
console.log(mixedArr[1]);  // Expected output: ["otherarray"]
console.log(mixedArr[2]);  // Expected output: 123
console.log(mixedArr[3]);  // Expected output: true
